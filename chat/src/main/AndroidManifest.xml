<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bridgefy.samples.chat">

    <application android:allowBackup="true" android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name" android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true" android:theme="@style/AppTheme">
        <activity android:name=".PeerListActivity" android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PeerDetailActivity" android:label="@string/title_peer_detail"
            android:parentActivityName=".PeerListActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value="com.bridgefy.samples.chat.PeerListActivity" />
        </activity>

        <meta-data android:name="com.bridgefy.sdk.API_KEY"
            android:value="254540ef-f130-463c-b13d-e2d2adcb8f3a"/>
    </application>

</manifest>